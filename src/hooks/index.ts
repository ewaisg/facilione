/**
 * Custom React hooks.
 * Will be populated as modules are built in Phases 2+.
 *
 * Planned hooks:
 * - useProject(projectId) — single project with real-time updates
 * - useProjects() — role-filtered project list
 * - usePhases(projectId) — real-time phase data
 * - useBudget(projectId) — budget line items
 */

export {}
