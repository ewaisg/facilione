/**
 * Project-specific components.
 * Will be populated as project detail modules are built in Phases 2+.
 *
 * Planned components:
 * - ProjectCard — used in list and dashboard
 * - PhaseAccordion — sequence tracker
 * - GateIndicator — gate pass/fail display
 * - HealthBadge — project health status
 */

export {}
